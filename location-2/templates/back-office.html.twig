<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
       <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css">
    </head>
    <body>
        <header class="bg-warning">
            <nav class="navbar container navbar-expand navbar-light mb-3">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a href="" class="nav-link">Back Office</a>
                    </li>
                </ul>
            </nav>
        </header>
        <div class="container">
            <section class="row" >
                <nav class="col-2">
                    <h2 class="fs-5 my-3">Gestion Véhicules</h2>
                    <div class="list-group">
                        <a href="{{ path("vehicule_new") }}" class="list-group-item list-group-item-action">Ajouter</a>
                        <a href="{{ path("vehicule_list") }}" class="list-group-item list-group-item-action">Liste</a>
                    </div>
                    <h2 class="fs-5 my-3">Gestion Membres</h2>
                    <div class="list-group">
                        <a href="{{ path("register_new") }}" class="list-group-item list-group-item-action">Ajouter</a>
                        <a href="{{ path("register_list") }}" class="list-group-item list-group-item-action">Liste</a>
                    </div>
                    <h2 class="fs-5 my-3">Gestion Commandes</h2>
                    <div class="list-group">
                        <a href="{{ path("commande_new") }}" class="list-group-item list-group-item-action">Ajouter</a>
                        <a href="{{ path("commande_list") }}" class="list-group-item list-group-item-action">Liste</a>
                    </div>
                </nav>
                <div class="col-10">
                    {% block body %}{% endblock %}
                </div>
            </section>
        </div>
    </body>
</html>
