{% extends "base.html.twig" %}

{% block title %}Accueil{% endblock %}

{% block body %}

    {% set prix = 12345 %}
    <p>{{ prix | prixFr(1.2) }}</p>
    <p>{{ prix | prixFr }}</p>
    <h1>page d'accueil <small class="fs-5"> {{ app.user ?  "bienvenu " ~ app.user.email : "" }}</small></h1>
    <h2>nb articles publiés : {{ articles.getTotalItemCount  }}</h2>
    {% for article in articles %}
       {% include "commun/list-articles.html.twig" %}
    {% endfor %}
    <div class="navigation d-flex justify-content-center my-3">
        {{ knp_pagination_render(articles) }}
    </div>
{% endblock %}